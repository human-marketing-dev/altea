import { EMBLEM } from "./content";

/** Emblema de Altea, a todo lo ancho. */
export function Emblem() {
  return (
    <section className="home-emblem">
      <p className="home-emblem__text">{EMBLEM}</p>
    </section>
  );
}
