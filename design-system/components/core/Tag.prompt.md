Uppercase pill label used to mark a business unit (Comercial / Industrial / Vivienda) or a status on a card.

```jsx
<Tag tone="coral">Comercial</Tag>
<Tag tone="outline">Disponible</Tag>
```

Keep `coral` rare — reserve it for the single most important tag in a group; default to `ink`/`outline` for the rest.
